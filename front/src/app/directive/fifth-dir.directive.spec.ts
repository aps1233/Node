import { FifthDirDirective } from './fifth-dir.directive';

describe('FifthDirDirective', () => {
  it('should create an instance', () => {
    const directive = new FifthDirDirective();
    expect(directive).toBeTruthy();
  });
});
