import { Final7Directive } from './final-7.directive';

describe('Final7Directive', () => {
  it('should create an instance', () => {
    const directive = new Final7Directive();
    expect(directive).toBeTruthy();
  });
});
