import { SixthLoopDirDirective } from './sixth-loop-dir.directive';

describe('SixthLoopDirDirective', () => {
  it('should create an instance', () => {
    const directive = new SixthLoopDirDirective();
    expect(directive).toBeTruthy();
  });
});
