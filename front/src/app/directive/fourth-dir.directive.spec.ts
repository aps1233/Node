import { FourthDirDirective } from './fourth-dir.directive';

describe('FourthDirDirective', () => {
  it('should create an instance', () => {
    const directive = new FourthDirDirective();
    expect(directive).toBeTruthy();
  });
});
